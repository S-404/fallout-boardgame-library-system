import React from 'react';

const GameSettings = () => {
    return (
        <div>
            game settings
            <button>1</button>
            <button>2</button>
            <button>3</button>
        </div>
    );
};

export default GameSettings;